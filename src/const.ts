export enum AppRoute {
  Home = '/',
  Login = '/login',
  Offer = '/offer',
  Favorites = '/favorites',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
